{
  "name": "ora-judge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env dev-server.js",
    "dev": "node --env-file=.env --watch dev-server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
